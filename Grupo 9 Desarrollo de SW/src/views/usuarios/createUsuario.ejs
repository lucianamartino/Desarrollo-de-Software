<%- include('../partials/_header'); %>
<main>
    <div class="container-principal">
        <div class="container-nombre-form">
            <h1 class="nombre-form">
                <span class="arriba">Crear</span>
                <span class="abajo">Usuario</span>
            </h1>
          
        </div>
        <div class="container-form">
            <form class="formulario" action="/api/usuarios/create" method="POST">
                    <div class="container-label">
                        <label for="nombreUsuario">Nombre de usuario:</label>
                        <input type="text" id="nombreUsuario" name="nombreUsuario" required>
                    </div>
                    <% if (error && error.nombreUsuario) { %>
                        <div class="error"><%= error.nombreUsuario %></div>
                    <% } %>

                    <div class="container-label">
                        <label for="email">Email:</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <% if (error && error.email) { %>
                        <div class="error"><%= error.email %></div>
                    <% } %>

                    <div class="container-label">
                        <label for="contraseña">Contraseña:</label>
                        <input type="password" id="contraseña" name="contraseña" required>
                    </div>


                <button type="submit">Crear Usuario</button>
            </form>
        </div>

        <!-- <% if(typeof alert !== 'undefined') { %>
            <script>
                Swal.fire({
                    title: '<%= alertTitle %>',
                    text: '<%= alertMessage %>',
                    icon: '<%= alertIcon %>',
                    showConfirmButton: <%= showConfirmButton %>,
                    timer: <%= timer %>
                }).then(()=> {
                    window.location='/<%=  %>'
                })
            </script>
        <% } %> -->
    </div>
</main>
<%- include('../partials/_footer'); %>
